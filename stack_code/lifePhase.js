// Write your function here:
const lifePhase = (age) => {
	switch (age) {
		case age > 0 && age <= 3:
			console.log('baby');
			break;
	}

	console.log(lifePhase(2));
	// let numb = 1;
	// let exp = numb > 0 && numb <= 3;
	// console.log(exp);
};
